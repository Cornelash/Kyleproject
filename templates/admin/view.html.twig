{{ include('admin/navbar.html.twig') }}


<div class="container">
    <h3>VIEW ITEM</h3>




    <div class="list-group">
        <a href="#" class="list-group-item active">Order</a>


        <p class="list-group-item">{{ post.placedby }}</p>
        <p class="list-group-item">{{ post.details }}</p>
        <p class="list-group-item">{{ post.location }}</p>
        <p class="list-group-item">{{ post.topay }}</p>
        <p class="list-group-item">{{ post.time }}</p>

        <p class="list-group-item"><a href="{{ absolute_url(asset('')) }}post" class="btn btn-success btn-sm">Back</a></p>
    </div>

</div>
